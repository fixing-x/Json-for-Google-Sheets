# JSON for Sheets - Store Listing

## Short Description (Max 80 characters)

Robust JSON parsing, filtering, and conversion formulas for Google Sheets™.

## Long Description

**Unleash the power of JSON directly in your spreadsheets.**

"JSON for Sheets" creates a seamless bridge between complex data structures and Google Sheets. Whether you are an SEO professional importing structured data, a developer analyzing API logs, or a data analyst working with configuration files, this add-on gives you the native-like tools you need.

### 🚀 Key Features

- **`=PARSE_JSON(json_string)`**: INSTANTLY convert JSON text into readable tables or key-value maps. No more manual copying and pasting.
- **`=JSON_GET(json_string, path)`**: Surgical precision extraction. Get exactly the data you need using dot notation (e.g., `user.address.city`).
- **`=JSON_TO_TABLE(json_array)`**: Automatically transform arrays of objects into structured, header-rich tables. Ideal for API response data.
- **`=JSON_FILTER(json_array, expression)`**: Filter data _before_ it hits your sheet. use simple logic like `price > 100` to keep your sheets clean and fast.
- **`=JSON_VALIDATE(json_string, schema)`**: Ensure your data integrity by validating JSON against custom schemas.

### 🔒 Secure by Design

Your data privacy is our priority. "JSON for Sheets" runs **entirely locally** within your Google Sheets execution environment. We do not transmit your data to external servers, ensuring your sensitive information stays within Google's secure infrastructure.

### 💼 Who is this for?

- **SEO Specialists**: Parse Schema markup and structured data from website crawls.
- **Marketers**: Analyze JSON exports from marketing tools and ad platforms.
- **Developers**: Debug API responses without leaving your spreadsheet workflow.
- **Data Analysts**: Flatten complex nested JSON logs into pivot-ready tables.

**Start controlling your JSON data today. Install "JSON for Sheets" now!**
